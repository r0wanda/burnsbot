[Unit]
Description=Mr Burns Discord Bot
After=network-online.target

[Service]
Type=notify
User=rowan
ExecStart=bash -c "cd /opt/burns && node ."
ExecStop=bash -c "cd /opt/burns && node dist/kill.js"
Restart=always
RestartSec=10

[Install]
WantedBy=default.target
